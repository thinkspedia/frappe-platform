# =============================================================================
# Frappe Platform — Dokploy Container Runtime Environment
#
# USAGE:
#   Paste the contents of this file into Dokploy UI:
#   Project → ERPNext App → Environment Variables
#
#   This file configures what runs INSIDE the containers.
#   It is NOT a local shell env file — do not `source` it.
#
#   For local `make` CLI secrets (API token, registry credentials),
#   see platform/.env.stg.example and platform/.env.prod.example.
# =============================================================================

# -----------------------------------------------------------------------------
# Image
# -----------------------------------------------------------------------------
IMAGE_REGISTRY=registry.corp.thinkspedia.id/frappe/nusakura
IMAGE_TAG=v1.0.0
PULL_POLICY=always
RESTART_POLICY=unless-stopped

# -----------------------------------------------------------------------------
# Site
# Set FRAPPE_SITE_NAME_HEADER to your production domain so nginx knows which
# site_config.json to load. Must match the site name created with bench new-site.
# -----------------------------------------------------------------------------
FRAPPE_SITE_NAME_HEADER=nusakura-stg.erp.thinkspedia.id

# Full public URL written into site_config.json as host_name.
# Used by Frappe for email link generation — must include protocol.
SITE_HOST_NAME=https://nusakura-stg.erp.thinkspedia.id

# Port that Dokploy's proxy routes to. Match this in Dokploy UI domain config.
HTTP_PUBLISH_PORT=8080

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DB_HOST=db
DB_PORT=3306
DB_ROOT_PASSWORD=CHANGE_ME

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379

# -----------------------------------------------------------------------------
# Gunicorn workers
# Formula: GUNICORN_WORKERS = (2 × CPU cores) + 1
# -----------------------------------------------------------------------------
GUNICORN_WORKERS=3
GUNICORN_THREADS=4
GUNICORN_TIMEOUT=120

# -----------------------------------------------------------------------------
# Migration — set to false to skip bench migrate on deploy
# -----------------------------------------------------------------------------
MIGRATE_SITES=true

# -----------------------------------------------------------------------------
# Nginx tuning
# -----------------------------------------------------------------------------
UPSTREAM_REAL_IP_ADDRESS=127.0.0.1
UPSTREAM_REAL_IP_HEADER=X-Forwarded-For
UPSTREAM_REAL_IP_RECURSIVE=off
PROXY_READ_TIMEOUT=120
CLIENT_MAX_BODY_SIZE=50m
